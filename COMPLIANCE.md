# 合规说明 / Compliance

> 🤖 **AI 生成声明**：本插件由「鲸鱼娘」（DeepSeek 驱动的 AI 助手）编写，代码与文档均由其完成。
> 作者（仓库所有者）仅提供需求与验收，质量责任由发布者自负。

本插件为**非官方第三方工具**，与 NovelAI / Anlatan 公司无任何关联、未获其背书。

## 使用前提
- 使用者需自行拥有有效的 **NovelAI 订阅**（Anlas 额度），并**使用自己的账号 Token**。
- 插件不代收、不共享、不转售任何账号或额度；所有请求均以使用者本人 Token 鉴权发出。

## 与 NovelAI 服务条款（ToS）的关系
本插件仅调用 NovelAI **官方公开 API 端点**（`https://image.novelai.net/ai/generate-image`），
与官方网页前端使用同一接口、同一鉴权方式，单次生成 1~4 张（`n_samples`，官方支持范围），
不属于「绕过访问限制」「规避技术保护」「自动化系统造成过度负担」等被禁止行为。

插件代码不包含 NovelAI 的任何专有资源（模型权重、官方前端代码、素材、立绘等）。

## 发布约定
- 仓库**不打包**任何 NovelAI 素材；模型 ID 仅为公开 API 参数名。
- 仓库**禁止提交**任何真实 Token / 密钥（见 `.gitignore`）。
- 请勿基于本插件搭建「共享账号 / 免费代生成」类公共服务——那将同时违反
  ToS 9.1.3（允许他人使用你的账号）与 9.1.6（对服务造成过度负担），责任自负。

## 免责声明
使用本插件产生的任何账号风险、额度消耗或与 NovelAI 服务条款的争议，由使用者自行承担。
